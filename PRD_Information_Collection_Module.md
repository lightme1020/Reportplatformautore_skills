# AutoRe 信息采集模块 (Information Collection) PRD

## 1. 引言与背景 (Introduction)
### 1.1 模块定位
“信息采集”模块是 AutoRe 报告自动生成流程中的首要“数据入口”（Ingest & Parse）。其核心承载非结构化/半结构化源文件（如 PDF、扫描件图片等）的集中上传、处理、解析与持久化操作，为主体报告生成提供精确、合规的底层结构化 JSON 数据支撑。

### 1.2 核心应用场景
检测实验室在出具报告前，往往需汇总大量设备打印或外部供应商提供的原始记录单（如混凝土抗压记录、砂浆记录、软件计算书等）。本模块通过调用不同的声明式提取技能（Declarative Skills），自动化识别并提取关键数据项，极大地替代了传统的手工录入过程，提升处理时效并降低人为誊转错误率。

---

## 2. 全局交互与界面框架 (UI & Layout)
本模块主要以全屏/大尺寸弹窗（或沉浸式页面）展示，采用典型的 **“左阅右填（Left-Reading, Right-Editing）”** 分屏结构，为用户提供极佳的沉浸式校对体验。

### 2.1 整体布局结构
*   **顶部状态/操作栏 (Header Bar)**：
    *   **模块名称**：展示“多文档智能上传”等引导性标题。
    *   **全局操作**：提供文件上传入口（按钮/下拉列表）、整体进度/状态提示（如“等待自动识别结果”）。
*   **左侧侧边栏：源文件阅读器 (Source Document Viewer)**：
    *   提供大面积的文档查阅区域。
    *   渲染用户选中的 PDF 文件或图片，支持分页浏览、缩放等富交互式阅读控件。
*   **右侧侧边栏：数据解析与工作台 (Data Extraction & Workspace)**：
    *   以文件（或检测项目）为维度的管理面板。
    *   展示当前选中文件的抽取状态，以及转换后的可视化结构数据（支持表单或表格形态的直接修改）。

---

## 3. 核心功能需求 (Core Functions)

### 3.1 多文件智能上传与队列管理 (Upload & Queue Management)
*   **文件上传机制**：
    *   支持多文档批量选取和拖拽上传。
    *   限制并校验格式类型（PDF、常见图片格式等）及单文件大小限制。
*   **文件状态机管理**：
    所有进入采集系统的文件必须包含清晰的状态扭转：
    1.  `pending` (等待上传/排队中)
    2.  `uploading` (上传中)
    3.  `解析中` (后端技能处理中)
    4.  `已解析`/`uploaded` (处理成功，前端已渲染结构化数据)
    5.  `failed` (处理失败，需提示具体原因)
*   **文件列表管理**：
    *   提供已上传任务的下拉列表或折叠面板以供随时切换。
    *   支持单文件删除操作（回收站 `Trash2` 图标）。
    *   对于失败的文件，需提供“重试 (Retry)”或重新处理的操作入口。

### 3.2 信息抽取引擎与技能调度 (Skill Execution & Extraction)
*   **解析技能匹配 (`SkillSelector`)**：
    *   当上传或选中特定文件时，用户可通过技能选择器（Skill Selector）指定该文件对应的声明式规则/技能（如 `concrete_table_recognition`, `software_calculation_recognition` 等）。
*   **智能数据结构化展示**：
    *   前端需要动态承接来自后端的 `parse_result`（JSON 数据）。
    *   **表格视图 (Table View)**：当返回的 JSON 为数组结构（Array of Objects，如同一份文件的多行检测数据）时，自动渲染为数据表格，包含表头和相应的行数据。
    *   **表单视图 (Form View)**：当返回的 JSON 为扁平对象（Key-Value 对象，如基本信息表）时，自动渲染为垂直或两栏布局的表单（Input/Textarea）。

### 3.3 数据在线校验与人工修正 (Human-in-the-loop Validation)
*   **自动校验反馈展示**：
    *   接收解析服务下发的 `validation_result`。
    *   如 `is_valid` 为 `false`，需将 `errors` (严重错误) 和 `warnings` (异常项/置信度低) 在对应的表单字段或表格单元格周遭进行高亮标识与文本提示。
*   **在线编辑与修改 (Human-in-the-loop)**：
    *   渲染出的 Form 表单与 Table 表格均需支持**双向绑定（可交互）**。用户可直接点击数据输入框，参照左侧的原文预览，对 OCR 错误或遗漏的数据进行手动覆写和更新。
*   **数据确认与持久化入库**：
    *   提供明显的【确认并保存】操作按钮。
    *   所有抽取和修改的动作均先停留在**前端内存/缓存**中。只有在用户点击确认后，最终清洗后的 `mapping_payload` 与验证结果才会发送给后端并正式沉淀入数据库，避免过程垃圾数据污染业务库。

### 3.4 源文件高级预览功能 (Native PDF & Image Viewer)
*   **PDF.js 原生渲染支持**：
    *   集成 `pdf.js` Worker，实现浏览器端纯前端渲染 PDF 文件，避免依赖外部插件。
    *   支持按需加载（当前页渲染），并将已渲染的 Canvas 图像进行 **Key-Value 内存缓存 (`renderCacheKey`)**，使得用户在频繁上下翻页或切换左右面板时实现“零延迟”展示。
*   **阅读器辅助控件**：
    *   提供包含：放大 (`ZoomIn`)、缩小 (`ZoomOut`)、重置大小/居中、旋转 (`RotateCcw`) 及上一页/下一页等标准阅读器辅助工具。

---

## 4. 异常与边界处理 (Exception & Boundary Handling)

### 4.1 放呆防误触与退出保护 (Exit Protection)
*   **场景判断**：当用户尝试关闭“信息采集”弹窗或驶离当前页面时，组件必须检查当前是否存在 `已解析` 但 **尚未确认 (Unconfirmed)** 的文件列表。
*   **拦截提示**：若存在遗留的未确认文件，需使用 Modal 或 Dialog 强拦截操作，并明确告知用户（例如：“有 N 个文件已解析但尚未确认：[文件名]。未确认的文件不会保存到数据库，确定要关闭吗？”），防止劳动成果意外丢失。

### 4.2 解析异常与回显拦截 (Parse Error Handling)
*   **JSON 格式失效**：若大模型或工具返回的内容前端无法 `JSON.parse`，页面需提供兜底的 Toast/Alert 拦截，提示：“JSON 格式错误：无法解析”，并阻止应用白屏崩溃。
*   **状态标红**：对于完全处理失败的文件，列表项标记 `failed`，并可展示简要错误堆栈或 `other` 失败原因。

### 4.3 源文件缺失兜底 (Missing Source File)
*   如果前端拿到的文件记录 ID 缺乏合法的 `url` 预览地址，试图在左侧预览区渲染时，必须显示错误占位页，提示：“文件预览地址缺失，请重新上传该文件。”。

---

## 5. 非功能与技术约束 (Non-Functional Constraints)

### 5.1 明确的接口契约依赖 (API Contract Dependency)
*   前端高度依赖后端接口返回的标准数据形态，核心接口字段模型底线需包含：
    *   `status` (处理状态)
    *   `parse_result` (业务数据 Playload)
    *   `validation_result` (置信度和校验状态载体)
    *   `url` / `object_key` (源文件的对象存储提取路径)

### 5.2 渲染性能与环境要求 (Performance & Rendering)
*   **长列表渲染限制**：当单次解析结果超过数百行表格数据时，前端应考虑渲染性能卡顿问题。
*   **PDF Worker 隔离**：要求采用 `pdf.worker.min.mjs` 在独立线程处理文件解析与位图转换任务，以保证在进行大体积报告文件拖动浏览时，右侧表单输入与主线程渲染不掉帧。